use clap::{App, Arg};

//use lp::db::Compaction;

fn main() {
    /*
    let app = App::new("zataods-lp-db-compaction-perform")
        .version("0.1.0")
        .about("Process a previously-setup compaction.");
    let app = app.arg(
        Arg::with_name("compaction")
            .index(1)
            .multiple(false)
            .required(true)
            .help("Direct path to the compaction to complete.  Requires external mutual exclusion."));

    // parse
    let args = app.get_matches();
    let compaction = args.value_of("compaction").unwrap();
    let compaction = Compaction::load(compaction).unwrap();

    // TODO(rescrv): run compaction here
    */
}
