[package]
name = "rc_conf_demo_crate"
version = "0.3.0"
authors = ["Robert Escriva <robert@rescrv.net>"]
edition = { workspace = true }
description = "rc_conf_demo_crate provides an example crate for _SPEC testing"
license = "Apache-2.0"
repository = "https://github.com/rescrv/blue"

[features]
default = ["binaries"]

binaries = []

[dependencies]

[[bin]]
name = "rcdemo"
path = "src/main.rs"
required-features=["binaries"]
